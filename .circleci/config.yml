version: 2
jobs:
   build:
     docker:
       - image: circleci/node:latest
     steps:
       - checkout
       - run: npm install
       - run: npm run build
       - run: git log --oneline --decorate > build/changelog.txt
       - run: node deploy_server.js
       - run: node deploy_app.js
       - run: devil www restart furyroad.wroblewskipiotr.pl

workflows:
  version: 2
  deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
               - master