version: 2
jobs:
   build:
     docker:
       - image: circleci/node:latest
     steps:
       - checkout
       - run: npm install
       - run: npm install --prefix ./client
       - run: npm install --prefix ./server
       - run: npm run build --prefix ./client
       - run: npm run build --prefix ./server
       - run: git log --oneline --decorate > client/public/changelog.txt
       - run: cp server/package.json server/build/
       - run: node deploy_server.js
       - run: node deploy_app.js
       - run: sudo apt-get update && sudo apt-get install -y sshpass
       - run: sshpass -e ssh szczypiorofix@21.mydevil.net cd domains/furyroad.wroblewskipiotr.pl/public_nodejs/ && npm install && devil www restart furyroad.wroblewskipiotr.pl
workflows:
  version: 2
  deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
               - master